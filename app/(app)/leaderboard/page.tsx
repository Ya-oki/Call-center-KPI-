import { PhasePlaceholder } from "@/app/_components/PhasePlaceholder";

export default function LeaderboardPage() {
  return (
    <PhasePlaceholder title="Contribution Leaderboard" audience="manager" phase="P3">
      Per-agent rank, share, outcome/activity indices, conduct, payout and
      paid-now/held — sortable, searchable (mirrors the prototype Leaderboard).
    </PhasePlaceholder>
  );
}
