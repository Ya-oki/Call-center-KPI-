import { PhasePlaceholder } from "@/app/_components/PhasePlaceholder";

export default function MethodPage() {
  return (
    <PhasePlaceholder title="How it works" audience="manager" phase="P3">
      The model in plain language: Pool = net desk revenue × payout rate; Score =
      Conduct × (outcome-weight × Outcome + activity-weight × Activity). Includes
      the verbatim Withdrawals policy shown to agents.
    </PhasePlaceholder>
  );
}
