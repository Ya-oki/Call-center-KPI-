/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The /reference docs are canonical source-of-truth files, not build inputs.
  // Keep them out of the compiled app.
  experimental: {},
};

export default nextConfig;
