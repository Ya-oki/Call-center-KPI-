/**
 * Typed data-access helpers over Postgres (via Supabase now, portable later).
 *
 * P0 STUB — built in P2/P3. Keeps all SQL/queries behind this module so the
 * engine and UI never talk to the database directly. Standard Postgres only.
 */

export {};
